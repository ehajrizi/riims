import React from 'react';
import { scroller } from 'react-scroll';

export default function scrollToPublikimet() {
    scroller.scrollTo("publikimet", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToEksperiencat() {
    scroller.scrollTo("eksperiencat", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToEdukimi() {
    scroller.scrollTo("edukimi", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToSpecializimet() {
    scroller.scrollTo("specializimet", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToCertifikimet() {
    scroller.scrollTo("certifikimet", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToMbikeqyresITemave() {
    scroller.scrollTo("mbikeqyresitemave", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToProjektet() {
    scroller.scrollTo("projektet", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToAnetaresia() {
    scroller.scrollTo("anetaresia", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToHonorsAndAwards() {
    scroller.scrollTo("honorsandawards", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};

export function scrollToTop() {
    scroller.scrollTo("navbar", {
        duration: 800,
        delay: 0,
        smooth: "easeInOutQuart",
    });
};